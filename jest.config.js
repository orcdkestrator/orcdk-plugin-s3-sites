/** @type {import('jest').Config} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/src'],
  testMatch: ['**/__tests__/**/*.test.ts'],
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/**/__tests__/**',
    '!src/index.ts',
    '!src/s3-uploader.ts',          // Complex AWS SDK S3 integration
    '!src/cloudfront-invalidator.ts', // Complex AWS SDK CloudFront integration  
    '!src/stack-inspector.ts',      // Complex AWS SDK CloudFormation integration
    '!src/pattern-detector.ts'      // Complex file system scanning
  ],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  },
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1'
  },
  transform: {
    '^.+\\.ts$': ['ts-jest', {
      tsconfig: {
        sourceMap: true
      }
    }]
  }
};